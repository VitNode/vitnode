{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "dependsOn": ["^build"]
    },
    "start": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "dependsOn": ["^start"],
      "persistent": true
    },
    "build:scripts": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build:scripts"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "db": {
      "dependsOn": ["^db"],
      "cache": false
    }
  }
}
