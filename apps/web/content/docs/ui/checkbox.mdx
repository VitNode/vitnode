---
title: Checkbox
description: Toggle between checked and unchecked states.
---

## Preview

<Preview name="checkbox" />

## Usage

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

<Tabs items={['Auto Form', 'Manual']}>
  <Tab value="Auto Form">

```ts
import { z } from "zod";
import { AutoForm } from "@vitnode/core/components/form/auto-form";
import { AutoFormCheckbox } from "@vitnode/core/components/form/fields/checkbox";
```

```ts
const formSchema = z.object({
  acceptTerms: z.boolean().refine(val => val, {
    message: "You must accept the terms and conditions",
  }),
});
```

```tsx
<AutoForm
  formSchema={formSchema}
  fields={[
    {
      id: "acceptTerms",
      component: props => (
        <AutoFormCheckbox
          {...props}
          label="I accept the terms and conditions"
        />
      ),
    },
  ]}
/>
```

  </Tab>

  <Tab value="Manual">
```ts
import { Checkbox } from '@vitnode/core/components/ui/checkbox';
```

```tsx
<Checkbox onCheckedChange={onCheckedChange} checked={checked} />
```

  </Tab>
</Tabs>

## Props

import { TypeTable } from "fumadocs-ui/components/type-table";

<TypeTable
  type={{
    label: {
      description:
        "The label for the checkbox. This is displayed next to the checkbox.",
      type: "string",
      default: "",
    },
    description: {
      description:
        "A short description of the checkbox. This is displayed below the checkbox.",
      type: "string",
      default: "",
    },
  }}
/>

## API Reference

[Base UI - Checkbox](https://base-ui.com/react/components/checkbox#api-reference)
