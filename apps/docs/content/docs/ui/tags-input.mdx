---
title: Tag Input
description: Tag Input is a component that allows users to input tags.
---

## Preview

Image

## Usage

```tsx
const formSchema = z.object({
  tag_input_test: z.object({ id: z.number(), value: z.string() }),
});
```

```tsx
import { TagsInput } from 'vitnode-frontend/components/ui/tags-input';
```

```tsx
<FormField
  control={form.control}
  name="tag_input_test"
  render={({ field }) => (
    <FormItem>
      <FormLabel>Tag Input Test</FormLabel>
      <FormControl>
        <TagsInput {...field} /> // [!code highlight]
      </FormControl>
      <FormMessage />
    </FormItem>
  )}
/>
```

### Multiple

```tsx
const formSchema = z.object({
  tag_input_test: z.array(z.object({ id: z.number(), value: z.string() })),
});
```

```tsx
import { TagsInput } from 'vitnode-frontend/components/ui/tags-input';
```

```tsx
<FormField
  control={form.control}
  name="tag_input_test"
  render={({ field }) => (
    <FormItem>
      <FormLabel>Tag Input Test</FormLabel>
      <FormControl>
        <TagsInput {...field} multiple /> // [!code highlight]
      </FormControl>
      <FormMessage />
    </FormItem>
  )}
/>
```

## API Reference

import { TypeTable } from 'fumadocs-ui/components/type-table';

<TypeTable
  type={{
    placeholder: {
      description: 'Placeholder text.',
      type: 'string',
      default: '',
    },
    multiple: {
      description: 'Allow multiple tags.',
      type: 'boolean',
      default: 'false',
    },
    disabled: {
      description: 'Disable input.',
      type: 'boolean',
      default: 'false',
    },
  }}
/>
