---
title: Date Picker
description: Renders a date picker or an element designed to resemble a date picker.
---

import { ImageZoom } from 'fumadocs-ui/components/image-zoom';
import img from '@/assets/ui/components/date-picker.png';

<ImageZoom src={img} className="max-w-md" alt="Date Picker" />

## Usage

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<Tabs items={['Auto Form', 'Manual']}>
  <Tab value="Auto Form">
```ts
import * as z from 'zod';
import { AutoForm } from 'vitnode-frontend/form/auto-form';
import { AutoFormDate } from 'vitnode-frontend/form/fields/date';
```

```ts
const formSchema = z.object({
  birthday: z.date().default(new Date()),
});
```

```tsx
<AutoForm
  formSchema={formSchema}
  fieldConfig={{
    birthday: {
      label: 'Birthday',
      fieldType: AutoFormDate,
    },
  }}
/>
```

  </Tab>

  <Tab value="Manual">

```tsx
'use client';

import React from 'react';
import { DatePicker } from 'vitnode-frontend/components/ui/date-picker';

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date | undefined>(new Date());

  return <DatePicker value={date} onChange={setDate} />;
}
```

  </Tab>
</Tabs>

## Examples

### Controlled Month and Year

Enable dropdown for month and year.

```tsx
<AutoForm
  formSchema={formSchema}
  fieldConfig={{
    birthday: {
      label: 'Birthday',
      fieldType: (props: AutoFormInputComponentProps) => (
        <AutoFormDate
          {...props}
          controlledMouthAndYear={{
            fromYear: 1900, // [!code highlight]
            toYear: new Date().getFullYear(), // [!code highlight]
          }}
        />
      ),
    },
  }}
/>
```

## API Reference

import { TypeTable } from 'fumadocs-ui/components/type-table';

<TypeTable
  type={{
    onChange: {
      description: 'Callback when the value changes.',
      type: '(value: Date | undefiend) => void;',
    },
    value: {
      description: 'Value of the date picker.',
      type: 'Date | undefiend',
    },
    disabled: {
      description: 'Disable the date picker.',
      type: 'boolean',
    },
    controlledMouthAndYear: {
      description: 'Enable dropdown for month and year.',
      type: '{ fromYear: number; toYear: number; }',
    },
  }}
/>
