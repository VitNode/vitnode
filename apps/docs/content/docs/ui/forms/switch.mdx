---
title: Switch
description: A control that enables the user to toggle between two states, such as on and off.
---

import { ImageZoom } from 'fumadocs-ui/components/image-zoom';
import img from '@/assets/ui/components/switch.png';

<ImageZoom src={img} className="max-w-xs" alt="Switch" />

<Callout title="Shadcn UI">
  This component is part of [Shadcn
  UI](https://ui.shadcn.com/docs/components/switch).
</Callout>

## Usage

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

<Tabs items={['Auto Form', 'Manual']}>
  <Tab value="Auto Form">
```ts
import * as z from 'zod';
import { AutoForm } from 'vitnode-frontend/form/auto-form';
import { AutoFormSwitch } from 'vitnode-frontend/form/fields/switch';
```

```ts
const formSchema = z.object({
  allow_files: z.boolean(),
});
```

```tsx
<AutoForm
  formSchema={formSchema}
  fields={[
    {
      id: 'allow_files',
      component: AutoFormSwitch,
    },
  ]}
/>
```

  </Tab>

  <Tab value="Manual">
```ts
import { Switch } from 'vitnode-frontend/components/ui/switch';
```

```tsx
<Switch checked={value} onCheckedChange={onChange} />
```

  </Tab>
</Tabs>

## API Reference

[Radix UI Switch](https://www.radix-ui.com/primitives/docs/components/switch#api-reference).
