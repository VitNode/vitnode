import type { IconInputProps } from "../content";

interface Props extends IconInputProps {
  search: string;
}

export const EmojisContentIconInput = ({ search }: Props) => {
  return <div>EmojisContentIconInput</div>;
};
