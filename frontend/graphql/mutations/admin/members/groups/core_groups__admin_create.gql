mutation core_groups__admin_create($name: [TextLanguageInput!]!) {
  core_groups__admin_create(name: $name) {
    created
    id
    name {
      language_id
      value
    }
    protected
    users_count
    guest
    updated
  }
}
