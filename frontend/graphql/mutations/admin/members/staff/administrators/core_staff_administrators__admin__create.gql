mutation Core_staff_administrators__admin__create(
  $groupId: String
  $userId: String
  $unrestricted: Boolean!
) {
  core_staff_administrators__admin__create(
    group_id: $groupId
    user_id: $userId
    unrestricted: $unrestricted
  ) {
    created
    id
    protected
    unrestricted
    updated
  }
}
