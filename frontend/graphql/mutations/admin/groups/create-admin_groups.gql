mutation Create_admin_groups($name: [TextLanguageInput!]!) {
  create_admin_groups(name: $name) {
    created
    id
    name {
      id_language
      value
    }
    protected
    users_count
    guest
    updated
  }
}
