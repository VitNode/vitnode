mutation Admin__core_themes__create(
  $author: String!
  $authorUrl: String!
  $name: String!
  $supportUrl: String!
) {
  admin__core_themes__create(
    author: $author
    author_url: $authorUrl
    name: $name
    support_url: $supportUrl
  ) {
    id
  }
}
