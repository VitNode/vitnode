mutation Admin__core_themes__edit(
  $author: String!
  $authorUrl: String
  $id: Int!
  $name: String!
  $supportUrl: String!
) {
  admin__core_themes__edit(
    author: $author
    author_url: $authorUrl
    id: $id
    name: $name
    support_url: $supportUrl
  ) {
    id
  }
}
