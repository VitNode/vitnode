query Core_languages__show(
  $first: Int
  $last: Int
  $cursor: Int
  $search: String
) {
  core_languages__show(
    first: $first
    last: $last
    cursor: $cursor
    search: $search
  ) {
    edges {
      code
      default
      enabled
      id
      name
      protected
      timezone
      locale
      time_24
    }
    pageInfo {
      count
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
      totalCount
    }
  }
}
