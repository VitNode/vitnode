# Ordering

VitNode provide a ordering system to order the query result. The ordering system is based on the [Prisma filtering and sorting system](https://www.prisma.io/docs/concepts/components/prisma-client/filtering-and-sorting).

Here is an example of how to use the ordering system. We will use the `core_members` table as an example.
