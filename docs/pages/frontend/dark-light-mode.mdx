# Dark and Light Mode

We're support three theme modes: `light`, `dark` and `auto`.

VitNode uses `Talwind CSS` as default style. You can read more about it in [Talwind CSS Dark Theme Documentation](https://tailwindcss.com/docs/dark-mode).
