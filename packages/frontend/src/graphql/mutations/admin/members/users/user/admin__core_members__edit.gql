mutation Admin__core_members__edit(
  $email: String!
  $id: Int!
  $name: String!
  $newsletter: Boolean!
) {
  admin__core_members__edit(
    email: $email
    id: $id
    name: $name
    newsletter: $newsletter
  ) {
    id
  }
}
